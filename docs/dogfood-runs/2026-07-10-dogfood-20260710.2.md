# Dogfood Run: dogfood-20260710.2

## Build Identity

- Date: 2026-07-10
- Channel: beta / internal dogfood
- Root cause fixed: remote link failed on coworkers' Macs with `failed to spawn tunnel provider: No such file or directory` because the previous DMG depended on a locally installed `cloudflared`.
- Internal artifacts:
  - `artifacts/desktop-shell/Codex-Mobile-Bridge-beta.app.zip`
  - `artifacts/desktop-shell/Codex-Mobile-Bridge-beta.dmg`

## Fix

- `scripts/prepare-desktop-bundle.sh` now downloads or falls back to a local `cloudflared`, then bundles it at `Codex Mobile Bridge.app/Contents/Resources/bin/cloudflared`.
- Runtime tunnel binary discovery now prefers the bundled provider before Homebrew or PATH.
- `scripts/dogfood-smoke.sh` fails if the debug `.app` does not include an executable bundled `cloudflared`.

## Verification

- [x] `cargo test -p desktop-core tunnel -- --nocapture`
- [x] `scripts/dogfood-smoke.sh`
- [x] `target/debug/bundle/macos/Codex Mobile Bridge.app/Contents/Resources/bin/cloudflared --version`
- [x] `cd apps/desktop-shell && npm run tauri:build -- --bundles app,dmg`
- [x] `target/release/bundle/macos/Codex Mobile Bridge.app/Contents/Resources/bin/cloudflared --version`
- [x] `RELEASE_CHANNEL=beta scripts/collect-desktop-artifacts.sh artifacts/desktop-shell`
- [x] `scripts/check-release-gate.sh --channel beta --artifact artifacts/desktop-shell/Codex-Mobile-Bridge-beta.app.zip`
- [x] `scripts/check-release-gate.sh --channel beta --artifact artifacts/desktop-shell/Codex-Mobile-Bridge-beta.dmg`

## Distribution Note

Send coworkers the new `Codex-Mobile-Bridge-beta.dmg` from this run. They should replace the previous install before testing remote links again.
