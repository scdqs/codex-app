{
  "name": "@codex/desktop-shell",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 57325",
    "build": "tsc --noEmit && vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "prepare:bundle": "../../scripts/prepare-desktop-bundle.sh",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run prepare:bundle && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/qrcode": "^1.5.6",
    "jsdom": "^25.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
